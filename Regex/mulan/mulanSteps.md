checked for & to change to &amp; there were none
checked for < and > to change to &lt; and &gt; there were none
used regex "(?s)(.*?)(?:\n\s*\n|\z)" to wrap text in <sp> element
used regex "(?s)(^.*$)" to wrap text in root element <play>
saved .txt to .xml and pretty printed